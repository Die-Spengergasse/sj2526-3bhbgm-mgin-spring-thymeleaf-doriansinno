# instruction to run the project
by starting thymeleaf and then run the main class of the project and then open the browser and type http://localhost:8080/ to see the project running
in the browser  you can add various objekts into a database and then you can see the list of the objects in the database by clicking on the list button in the browser

if u want to delete a chilli u need to enter the id of hte chilli manually into the url . the first chilli has the id 1 the second the id 2  and so on . 

u are also able to delete all the chillis in the database by clicking on the delete button in the list 